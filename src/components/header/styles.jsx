import { makeStyles } from '@material-ui/core'

export default makeStyles((theme) => ({
  toolbar: {
    backgroundColor: 'white',
    marginTop: '-1%',
    marginBottom: '0',
  },
  appbar: {
    boxShadow: 'none',
    backgroundColor: 'white',
  },
  heading: {
    color: 'black',
  },
}))
